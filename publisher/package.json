{
  "name": "multicam-publisher",
  "version": "1.0.0",
  "description": "LiveKit publisher for multi-camera RTSP streams",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "watch": "tsc -w"
  },
  "dependencies": {
    "@livekit/rtc-node": "^0.5.0",
    "livekit-server-sdk": "^2.0.0",
    "yaml": "^2.3.4",
    "dotenv": "^16.3.1",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
