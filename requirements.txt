# =============================================================================
# NATIVE AI BACKEND - MULTI-CAMERA PERSON TRACKING SYSTEM
# =============================================================================
# IMPORTANT: PyTorch GPU requires special installation from index URL
# 
# Installation Steps:
# Bước 1: Cài PyTorch GPU từ index riêng
#   pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 torchaudio==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
#
# Bước 2: Cài các dependencies còn lại
#   pip install -r requirements.txt
# =============================================================================

# Core Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==12.0

# Computer Vision & AI
opencv-python==4.8.1.78
opencv-python-headless==4.5.5.64
numpy==1.26.4
scipy==1.11.4

# ONNX Runtime (choose one based on your GPU)
# For NVIDIA GPU (CUDA 12.x) - supports YOLO and OSNet inference
onnxruntime-gpu==1.23.2
# For CPU only (comment out above, uncomment below):
# onnxruntime==1.23.2

# Image Processing & Augmentation
albumentations==1.3.1
Pillow==10.2.0

# PyTorch Deep Learning Framework (for advanced features)
# NOTE: Install these FIRST using the command in header, NOT via pip install -r
# torch==2.5.1+cu121
# torchvision==0.20.1+cu121
# torchaudio==2.5.1+cu121

# Data & Utilities
pydantic==2.5.3
python-multipart==0.0.6
aiofiles==23.2.1
PyYAML==6.0
tqdm==4.63.0
gdown==4.5.1

# Logging & Monitoring
colorlog==6.8.0

# Optional: for testing
pytest==7.4.3
pytest-asyncio==0.21.1
